# SQLite path is fixed in prisma/schema.prisma as file:../data/killnode.db → website/data/killnode.db
# (No DATABASE_URL needed for the website Prisma schema.)

# Admin session (production: use strong random values)
ADMIN_USERNAME="admin"
ADMIN_PASSWORD="killnode2026"
ADMIN_SESSION_SECRET="change-me-minimum-sixteen-chars"

# GitHub Releases API (landing download matrix)
GITHUB_REPO_OWNER="Alaustrup"
GITHUB_REPO_NAME="killnode"
# Optional: increases API rate limit for server-side fetches
GITHUB_TOKEN=""
